<div class="row">
    <div class="col-sm-6 offset-sm-3">
        <h1>俳句一覧</h1>
        <%= link_to(sort_pages_path()) do%>
            <button class="btn btn-secondary">人気順へ</button>
        <% end %>
    </div>
</div>
<div id="haikus_block">
    <%= render partial: "pages/haikus_index",locals: {haikus:@haikus}%>
</div>

